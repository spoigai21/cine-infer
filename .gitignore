# Dataset: MovieLens license forbids redistribution. Never commit it.
data/

# Model weights and intermediate artifacts are regenerated by the pipeline.
models/
*.pt
*.parquet

# Environment
.venv/
.env
__pycache__/
*.pyc
.pytest_cache/
spark-warehouse/
metastore_db/
derby.log

# Airflow runtime state (docker compose)
airflow/logs/
airflow/db/

.DS_Store

# Airflow (Phase 8): its own environment + home, never committed
.venv-airflow/
airflow_home/
