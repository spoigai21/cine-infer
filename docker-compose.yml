# Local stack: Spark (standalone cluster) and the API. Airflow runs from .venv-airflow
# (make airflow-ui): its tasks need the project's environment, which a container doesn't have.
# Development runs Spark in local mode from .venv; this cluster is for running the pipeline
# unchanged against a "real" master (spark://spark-master:7077).
# data/ is mounted read-write but never baked into an image (MovieLens license).

x-spark: &spark
  image: apache/spark:3.5.7
  volumes:
    - ./data:/opt/cineinfer/data
    - ./src:/opt/cineinfer/src:ro

services:
  spark-master:
    <<: *spark
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    ports:
      - "8080:8080"   # Spark master UI
      - "7077:7077"

  spark-worker:
    <<: *spark
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    environment:
      SPARK_WORKER_MEMORY: 4g
      SPARK_WORKER_CORES: "4"
    depends_on:
      - spark-master

  api:
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    volumes:
      - ./models/serving:/app/models/serving:ro   # `make export` first
    ports:
      - "8000:8000"
