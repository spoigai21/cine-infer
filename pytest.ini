[pytest]
testpaths = tests
filterwarnings =
    # Third-party deprecations inside FastAPI's TestClient, not our code.
    ignore::DeprecationWarning:starlette.testclient
    ignore:Using `httpx` with `starlette.testclient` is deprecated
